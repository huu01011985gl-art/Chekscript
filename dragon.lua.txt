-- Dragon Spawner GUI - Ultra Colorful (made by YNhi)

local ScreenGui = Instance.new("ScreenGui")
local Frame = Instance.new("Frame")
local UICorner = Instance.new("UICorner")
local UIStroke = Instance.new("UIStroke")
local UIGradient = Instance.new("UIGradient")

local Title = Instance.new("TextLabel")
local Credit = Instance.new("TextLabel")
local Button = Instance.new("TextButton")
local ButtonCorner = Instance.new("UICorner")
local ButtonGradient = Instance.new("UIGradient")

-- Parent
ScreenGui.Parent = game.CoreGui

-- Frame chính
Frame.Parent = ScreenGui
Frame.Size = UDim2.new(0, 300, 0, 200)
Frame.Position = UDim2.new(0.35, 0, 0.3, 0)
Frame.Active = true
Frame.Draggable = true

-- Bo góc
UICorner.Parent = Frame
UICorner.CornerRadius = UDim.new(0, 18)

-- Gradient nền
UIGradient.Parent = Frame
UIGradient.Color = ColorSequence.new{
    ColorSequenceKeypoint.new(0, Color3.fromRGB(0, 170, 255)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(170, 0, 255))
}
UIGradient.Rotation = 45

-- Viền RGB
UIStroke.Parent = Frame
UIStroke.Thickness = 3

-- Title
Title.Parent = Frame
Title.Size = UDim2.new(1, 0, 0, 50)
Title.BackgroundTransparency = 1
Title.Text = "🐉 DRAGON SPAWNER"
Title.Font = Enum.Font.GothamBlack
Title.TextScaled = true
Title.TextColor3 = Color3.fromRGB(255,255,255)

-- Credit
Credit.Parent = Frame
Credit.Position = UDim2.new(0, 0, 0.28, 0)
Credit.Size = UDim2.new(1, 0, 0, 25)
Credit.BackgroundTransparency = 1
Credit.Text = "make by Meow Script"
Credit.Font = Enum.Font.Gotham
Credit.TextScaled = true
Credit.TextColor3 = Color3.fromRGB(255,255,255)

-- Button
Button.Parent = Frame
Button.Size = UDim2.new(0.75, 0, 0.3, 0)
Button.Position = UDim2.new(0.125, 0, 0.55, 0)
Button.Text = "SPAWN DRAGON"
Button.Font = Enum.Font.GothamBold
Button.TextScaled = true
Button.TextColor3 = Color3.fromRGB(255,255,255)

-- Bo góc nút
ButtonCorner.Parent = Button
ButtonCorner.CornerRadius = UDim.new(0, 12)

-- Gradient nút
ButtonGradient.Parent = Button
ButtonGradient.Color = ColorSequence.new{
    ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 0, 150)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(0, 255, 255))
}

-- Hiệu ứng hover
Button.MouseEnter:Connect(function()
    Button.Size = UDim2.new(0.8, 0, 0.32, 0)
end)

Button.MouseLeave:Connect(function()
    Button.Size = UDim2.new(0.75, 0, 0.3, 0)
end)

-- RGB chạy màu viền
spawn(function()
    while true do
        for i = 0, 1, 0.01 do
            UIStroke.Color = Color3.fromHSV(i,1,1)
            wait(0.03)
        end
    end
end)

-- Khi bấm nút
Button.MouseButton1Click:Connect(function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/MoziIOnTop/pro/refs/heads/main/DragonEastSpawner.lua"))()
end)
